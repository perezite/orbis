#include <iostream>

#include "../orbis/video/Mesh.h"

int main(int argc, char* args[])
{
	orb::Mesh mesh(orb::PrimitiveType::Triangles);
	/*mesh[0].vertex s= orb::Vertex(orb::Vector2f(-1.0f, -1.0f, orb::Color(1, 0, 0));
	mesh[1].vertex = orb::Vertex(orb::Vector2f( 1.0f, -1.0f, orb::Color(0, 1, 0));
	mesh[2].vertex = orb::Vertex(orb::Vector2f( 1.0f,  0.0f, orb::Color(0, 0, 1));
	
	// orb::Engine::instance()->run();
	orb::Engine()->run();			// Mal schauen, ob sogar das geht mit static Engine::operator()
	*/



}